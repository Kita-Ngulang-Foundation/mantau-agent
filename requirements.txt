# mantau-core isn't published anywhere yet -- install from the local sibling
# checkout (one level up now that this is a standalone repo under
# mantau-prototype/, not nested under mantau-backend-localdevice/). Once it
# has a remote and tests are green there, switch this to the pinned git SHA
# documented in mantau-core's own README.
-e ../mantau-core
# On-device fall detection (MediaPipe pose, fall rules, ONNX classifier) --
# what MANTAU_DETECTOR_BACKEND=mediapipe runs, through mantau-core's adapter.
-e ../mantau-AI

-e .

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
